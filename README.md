# Spectral-Subtraction

An implementation of Spectral Subtraction via simulated sound sources and real .wav audio files.

numpy, matplotlib, and scipy are required modules

Note that there are "artifacts" or "musical noise" because sometimes, under or over subtraction can occur, leaving behind residual noise or periods of silence. 

This leads to a sort of underwater effect.
